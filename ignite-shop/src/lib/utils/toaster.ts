import { gray } from 'tailwindcss/colors'

export const toasterClasses = 'bg-gray-800 text-gray-100 border border-gray-600'
export const toasterStyles = {
  backgroundColor: gray[800],
  color: gray[100],
  border: `1px solid ${gray[600]}`,
}
