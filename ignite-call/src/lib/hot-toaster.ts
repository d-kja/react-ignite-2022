import { theme } from '@ignite-ui/react'

export const toasterConfig = {
  style: {
    background: theme.colors.gray700 as any,
    color: theme.colors.gray100 as any,
    fontFamily: theme.fonts.default as any,
  },
}
